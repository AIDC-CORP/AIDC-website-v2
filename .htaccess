<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Nếu request không phải là file hoặc thư mục thực sự
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # Chuyển hướng tất cả request về index.html
  RewriteRule ^ index.html [L]
</IfModule>
